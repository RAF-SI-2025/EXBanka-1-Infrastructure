apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ .Release.Namespace }}
  labels:
    app: frontend
    component: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: {{ .Values.port }}
      targetPort: http
      name: http
